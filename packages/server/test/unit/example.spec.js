import { describe, test } from 'node:test'
import assert from 'node:assert/strict'

describe('Example Unit Test', async () => {
  test('work', () => {
    assert.ok(true, 'This should always pass')
  })
})
